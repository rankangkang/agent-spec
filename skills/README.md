# Skills（技能）

此目录存放可复用技能（Skill）的定义与资源文件。

推荐结构：

```text
skills/
  <skill-name>/
    SKILL.md
    scripts/
    references/
    assets/
```

- **示例**：本仓库已提供 `git-worktree-workflow`，位于 `skills/git-worktree-workflow/`。
